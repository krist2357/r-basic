---
title: "ejemplodemarkdown"
author: "Christian Cadena"
date: "9/1/2020"
output:
  html_document:
    df_print: paged
  pdf_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

### Nuestros Chunks
Vamos a calcular $\sqrt{2} - e^{2}$:
```{r mi primera chunk, echo=TRUE, eval=TRUE, comment=NA}
sqrt(2) - exp(2)
```
Cuando queremos hacer la raiz cuadrada de dos, podemos hacerlo:


- En \LaTeX: $\sqrt{2}$
- En `R` haciendo `r sqrt(2)`
- La frase completa: $\sqrt{2}=$ `r sqrt(2)`

**negrita**

Hola

Ecuaciones en la línea $S = \pi\cdot r^2$

imagen ![](../R/r-basic-master/ejercicios/ej1.png)


Aqui empezamos otra cosa:

>Esto es una cita en bloque

### LISTAS ORDENADAS

1. Item primero
2. Item Segundo
    + subitem 2.1
    + subitem 2.2
3. Item tercero

Alumno    |    Nota
----------|--------
Juan      |   7.0
Chris     |   8.8



